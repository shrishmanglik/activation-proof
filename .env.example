# ActivationProof runs fully offline with synthetic fixtures by default.
# These variables are proposed for a separately authorized persistence phase.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
